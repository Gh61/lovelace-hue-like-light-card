name: Eslint
on: push
jobs:
  eslint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v3
    - name: Install modules
      run: npm ci
    - name: Run linter
      run: npm run lint